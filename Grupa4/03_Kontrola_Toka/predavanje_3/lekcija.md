# Predavanje 3: Višestruki Izbor uz switch Naredbu

## Teorijsko Objašnjenje
Naredba `switch` je efikasnija alternativa za `else-if` lance kada se vrednost jedne celobrojne promenljive ili karaktera upoređuje sa više konstantnih vrednosti (`case`). Ključna reč `break` prekida dalje izvršavanje.
