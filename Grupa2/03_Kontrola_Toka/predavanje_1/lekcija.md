# Predavanje 1: Uslovno Grananje sa if i if-else

## Teorijsko Objašnjenje
Naredba `if` omogućava uslovno izvršavanje blokova koda na osnovu logičkog izraza. Ako je izraz tačan (različit od nule), izvršava se `if` blok, u suprotnom se izvršava opciono `else` blok.
