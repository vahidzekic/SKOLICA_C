# Predavanje 1: Otvaranje i Zatvaranje Fajlova

## Teorijsko Objašnjenje
U C-u rad sa fajlovima ide preko pokazivača na `FILE` strukturu. Funkcija `fopen` otvara fajl u određenom režimu rada (`"r"` za čitanje, `"w"` za pisanje, `"a"` za dodavanje), a `fclose` ga zatvara.
