# Predavanje 1: Stringovi i Učitavanje sa fgets

## Teorijsko Objašnjenje
String u C-u je običan niz karaktera koji se završava null-karakterom (`'\0'`). Za bezbedan unos stringova sa tastature koristi se funkcija `fgets` umesto zastarele `gets`.
