# Predavanje 3: Čitanje iz Fajla sa fscanf i fgets

## Teorijsko Objašnjenje
Za čitanje iz fajla koristimo `fscanf` (za formatirani unos) ili `fgets` (za čitanje liniju po liniju). Obavezno je proveravati kraj fajla.
